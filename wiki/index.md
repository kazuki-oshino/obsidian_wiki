---
title: "Wiki Index"
type: "overview"
sources: []
created: 2026-05-13
updated: 2026-05-13
tags:
  - "wiki"
  - "overview"
  - "index"
  - "knowledge-base"
---

# 概要

このページは `wiki/` 全体の内容ベースの入口です。時系列の記録ではなく、各ノートが何を扱っているかを短く把握し、次に読むページを選ぶために使います。ノート本体は `wiki/source-summaries/`、`wiki/concepts/`、`wiki/comparisons/`、`wiki/overviews/` に type 別で配置します。

## 使い方

- 新しいソースを `raw/` に追加したら、対応する `source-summary` を `wiki/source-summaries/` に作成または更新する。
- 複数ソースを横断する整理が増えたら、`concept`、`overview`、`comparison` のいずれかとして再利用できる形にまとめる。
- ingest 時や大きな構造変更のあとに、このページへ主要ノートへの入口を追加する。

## 主要ノート

まだありません。

## Source Summaries

まだありません。

## 関連メモ

- `wiki/index.md` は ingest 時と大きな構造変更時に更新し、root に残す。
- `source-summary` は `wiki/source-summaries/`、`concept` は `wiki/concepts/`、`comparison` は `wiki/comparisons/`、個別テーマの `overview` は `wiki/overviews/` に置く。
- `source-summary` と `concept` を基本にし、`overview` と `comparison` は必要時のみ追加する。
- `log.md` はノート数や運用イベントが増えてから導入を検討する。
